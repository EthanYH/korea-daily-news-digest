# 복사해서 .env 로 만들기:  cp .env.example .env
export TELEGRAM_BOT_TOKEN=          # 선택. 비우면 파일 저장만
export TELEGRAM_CHAT_ID=            # 선택
export NEWS_KEYWORDS=코스피,비트코인,인디게임   # 선택. 비우면 종합 헤드라인
export MAX_PER_TOPIC=5              # 선택. 주제당 기사 수